///
/// HybridSslManagerSpecSwift.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include "HybridSslManagerSpec.hpp"

// Forward declaration of `HybridSslManagerSpec_cxx` to properly resolve imports.
namespace NitroSslManager { class HybridSslManagerSpec_cxx; }

// Forward declaration of `SslPinningConfig` to properly resolve imports.
namespace margelo::nitro::sslmanager { struct SslPinningConfig; }

#include <NitroModules/Promise.hpp>
#include "SslPinningConfig.hpp"
#include <string>
#include <vector>
#include <unordered_map>

#include "NitroSslManager-Swift-Cxx-Umbrella.hpp"

namespace margelo::nitro::sslmanager {

  /**
   * The C++ part of HybridSslManagerSpec_cxx.swift.
   *
   * HybridSslManagerSpecSwift (C++) accesses HybridSslManagerSpec_cxx (Swift), and might
   * contain some additional bridging code for C++ <> Swift interop.
   *
   * Since this obviously introduces an overhead, I hope at some point in
   * the future, HybridSslManagerSpec_cxx can directly inherit from the C++ class HybridSslManagerSpec
   * to simplify the whole structure and memory management.
   */
  class HybridSslManagerSpecSwift: public virtual HybridSslManagerSpec {
  public:
    // Constructor from a Swift instance
    explicit HybridSslManagerSpecSwift(const NitroSslManager::HybridSslManagerSpec_cxx& swiftPart):
      HybridObject(HybridSslManagerSpec::TAG),
      _swiftPart(swiftPart) { }

  public:
    // Get the Swift part
    inline NitroSslManager::HybridSslManagerSpec_cxx& getSwiftPart() noexcept {
      return _swiftPart;
    }

  public:
    inline size_t getExternalMemorySize() noexcept override {
      return _swiftPart.getMemorySize();
    }
    bool equals(const std::shared_ptr<HybridObject>& other) override {
      if (auto otherCast = std::dynamic_pointer_cast<HybridSslManagerSpecSwift>(other)) {
        return _swiftPart.equals(otherCast->_swiftPart);
      }
      return false;
    }
    void dispose() noexcept override {
      _swiftPart.dispose();
    }
    std::string toString() override {
      return _swiftPart.toString();
    }

  public:
    // Properties
    

  public:
    // Methods
    inline std::shared_ptr<Promise<void>> setUseSSLPinning(bool usePinning) override {
      auto __result = _swiftPart.setUseSSLPinning(std::forward<decltype(usePinning)>(usePinning));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<bool>> getUseSSLPinning() override {
      auto __result = _swiftPart.getUseSSLPinning();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<void>> setSSLConfig(const SslPinningConfig& config) override {
      auto __result = _swiftPart.setSSLConfig(std::forward<decltype(config)>(config));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<std::vector<std::string>>> getPinnedDomains() override {
      auto __result = _swiftPart.getPinnedDomains();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }

  private:
    NitroSslManager::HybridSslManagerSpec_cxx _swiftPart;
  };

} // namespace margelo::nitro::sslmanager
